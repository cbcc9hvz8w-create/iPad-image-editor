# SnapCanvas v1.4.0 安定性監査

## 監査結果

| 観点 | 改善前の主なリスク | 現在の対策 |
|---|---|---|
| データ消失 | 編集状態がメモリだけにあり、終了で消える | IndexedDBへ世代番号付きで自動保存し、起動時に復元 |
| 保存競合 | 古い保存が新しい編集を上書きする可能性 | 保存キュー、リビジョン、トランザクションで順序を保証 |
| 更新失敗 | 古いキャッシュが残り、修正が画面へ反映されない | 全ファイルのキャッシュ完了後に新版を自動有効化し、直前の安定キャッシュも保持 |
| 互換性 | 保存形式の移行経路がない | スキーマバージョンと段階マイグレーション、移行前バックアップ |
| 大容量処理 | 一括出力を最後までメモリへ保持 | 1枚ずつ処理・保存し、処理済み位置をチェックポイント化 |
| 重複操作 | 書き出し連打で処理が重なる | 実行中ロックと同一ファイルのサンプルハッシュ判定 |
| 画像リサイズ | 画像ではなくキャンバス内の縮小率が保存され、外周余白が残る | 旧縮小率を実寸pxへ変換し、保存時は位置とキャンバス倍率を必ず正規化 |
| タッチ操作 | 2本指の角度変化や縮小で回転・余白が発生 | ピンチを実寸px変更専用にし、1本指移動による位置ずれも停止 |
| エラー復旧 | 失敗理由が一律で、復旧方法が分からない | 容量・メモリ・通信・破損・形式・DB・更新等を分類して案内 |

## 検証済み

- TypeScript型検査（アプリ版・GitHub Pages版）
- 自動テスト15件
- 実画像2枚で、全面画像は593×640のまま維持し、背景付き623×640画像は実画像284×294へ検出
- 読み込み直後・保存済み作業の再開・数値入力・プリセットの外周除去経路
- 添付の背景付き623×640画像を実画像293×301として検出
- `IMG_4350.PNG`の623×640キャンバス内に残った約395×405画像を特定し、縮小率を実寸へ変換する回帰テストを追加
- ダウンロード保存ボタンが画面に存在しないことと、写真共有への統一
- 新Service Workerの自動有効化と、古い画面の自動再読み込み
- 再読み込み後の画像・編集寸法・自動保存状態の復元
- 更新情報・画面・Service Workerのバージョン一致
- GitHub Pages用静的ビルドとオフライン用ファイル構成
- ブラウザーのコンソール警告・エラーなし

## 残るプラットフォーム制約

- PWAからiPadの写真ライブラリへ無確認で直接書き込むことはできません。右上の「写真へ保存」で設定画面を省き、Appleの共有画面を1回で開きます。
- 実機iPadのOSによるメモリ回収やホーム画面PWA終了は、配布後に対象端末で最終確認が必要です。異常終了後の復元経路は実装済みです。
